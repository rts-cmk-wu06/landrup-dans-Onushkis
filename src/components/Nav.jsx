import { DefaultRoutes } from "../Routes";
import { NavLink } from "react-router-dom";

export const AppNavbar = () =>{

    return (
        <nav className="flex">
<h1>nav</h1>
        </nav>
    )
}